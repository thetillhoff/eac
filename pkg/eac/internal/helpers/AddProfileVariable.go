package helpers

import "errors"

func AddProfileVariable(key string, value string) error {

	return errors.New("not implemented")

	// Fail if not on linux or darwin

	// Fail if $HOME/.profile does not exist

}
